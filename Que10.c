//Write a program to calculate area of a rectangle. Input appropriate data from the user
#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Area of a rectangle are");
    scanf("%d%d",&a,&b);
c = a*b;
printf("length of rectangle are %d and breath is %d and reusult are %d",a,b,c);
return 0;
}
