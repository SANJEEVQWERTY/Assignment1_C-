//write a number to print cube are
#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter a number whose cube are");
    scanf("%d",&a);
    b= a*a*a;
    printf("whose number are %d",b);
    getch();
    return 0;
}