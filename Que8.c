//write a program to calculate square of a number
#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter a number whose square are");
    scanf("%d",&a);
    b= a*a;
    printf("these are %d",b);
    getch();
    return 0;
}